---
import Layout from '../../layouts/Layout.astro';
import { SvgConverter } from '../../components/converters/SvgConverter';
import { PostHogBoundary } from '../../components/PostHogBoundary.tsx';
---

<Layout 
  title="SVG to PNG Converter - Convert SVG to PNG, JPEG, WebP, AVIF" 
  description="Free online SVG converter. Convert SVG files to PNG, JPEG, WebP, or AVIF formats with custom dimensions and quality. No upload required - 100% client-side processing."
  sourceFormat="svg"
  targetFormat="png"
  toolType="image"
>
  <PostHogBoundary client:load>
    <SvgConverter />
  </PostHogBoundary>
</Layout>
