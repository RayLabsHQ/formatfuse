---
import Layout from '../../layouts/Layout.astro';
import PasswordGenerator from '../../components/converters/PasswordGenerator';

const title = 'Password Generator - Create Strong Secure Passwords';
const description = 'Generate strong, secure passwords with customizable options. Choose length, character types, and avoid ambiguous characters. Bulk generation and strength indicator included.';
---

<Layout
  title={title}
  description={description}
  keywords="password generator, secure password, strong password, random password generator, bulk password generator"
  toolType="default"
>
  <PasswordGenerator client:load />
</Layout>